// Free에서 파이프라인 편집을 막는 문장. 서버 액션의 거절과 레일의 선제 안내가 같은 문장이어야 한다 —
// 둘이 갈리면 사용자는 클릭 전과 후에 다른 말을 읽는다.
export const PIPELINE_EDIT_PLAN_GATE = "Pipeline editing opens on Pro. The default pipeline stays as is.";
